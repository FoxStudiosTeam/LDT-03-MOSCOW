apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: attachmentproxy-app
    release: kube-prometheus-stack
  name: attachmentproxy-app-monitor
  namespace: solution
spec:
  endpoints:
    - basicAuth:
        password:
          key: password
          name: attachmentproxy-metrics-auth
        username:
          key: username
          name: attachmentproxy-metrics-auth
      interval: 15s
      path: /metrics
      port: http
  namespaceSelector:
    matchNames:
      - attachmentproxy
  selector:
    matchLabels:
      app: attachmentproxy-app
